# Gentle AI cache local
.atl/.skill-registry.cache.json

# Dependencies
node_modules/

# Next.js
.next/
out/

# Production build
build/

# Environment variables
.env
.env.*
!.env.example

# Testing / coverage
coverage/

# TypeScript
*.tsbuildinfo

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/

# Catalog import snapshot (local artifact, never committed — see design D6).
# Anchored to the repo root on purpose: an unanchored `data/` also matched
# `src/lib/data/`, silently ignoring every new DAL file.
/data/
# Local AI runtime state
.atl/
